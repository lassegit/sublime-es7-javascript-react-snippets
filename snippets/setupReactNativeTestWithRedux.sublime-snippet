<snippet>
<content><![CDATA[import 'react-native'
import React from 'react'
import renderer from 'react-test-renderer'
import { Provider } from 'react-redux'

import store from 'src/store'
import ${1:${TM_FILENAME/(.+)\..+|.*/$1/:name}} from '../${1:${TM_FILENAME/(.+)\..+|.*/$1/:name}}'

describe('<${1:${TM_FILENAME/(.+)\..+|.*/$1/:name}} />', () => {
  const defaultProps = {}
  const wrapper = renderer.create(
    <Provider store={store}>
      <${1:${TM_FILENAME/(.+)\..+|.*/$1/:name}} {...defaultProps} />
    </Provider>,
  )

  test('render', () => {
    expect(wrapper).toMatchSnapshot()
  })
})
]]></content>
<tabTrigger>snrtest</tabTrigger>
<scope>source.js</scope>
<description>setupReactNativeTestWithRedux</description>
</snippet>